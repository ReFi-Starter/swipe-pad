{
  "name": "swipe-pad-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=apps/farcaster",
    "dev": "npm run dev --workspace=apps/farcaster",
    "install:all": "npm install --legacy-peer-deps"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
